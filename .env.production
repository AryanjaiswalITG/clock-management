# Backend API base URL used by the production build (GitHub Pages).
# Replace the host below with YOUR Render service URL, keeping the "/api" suffix.
# Find it on your Render dashboard after the backend deploys.
VITE_API_URL=https://clock-management-api.onrender.com/api
